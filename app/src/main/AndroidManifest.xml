<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- v1.9.10: 前台 service 权限，让 app 切后台时不被 Android 14+ 强制挂起子进程（dwg2dxf 等） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- v1.9.19 后台统计根治①：Android 13+ 若不声明并申请 POST_NOTIFICATIONS，前台 service 的
         通知根本不显示；国产 ROM(小米/华为/OPPO/vivo)会把"无可见通知的前台服务"当普通后台进程
         处理，切后台立刻冻结/回收 → 表现为"切后台不统计"。 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- v1.9.19 后台统计根治②：切后台且息屏后 CPU 进入 Doze/休眠，统计线程被挂起。
         必须持 PARTIAL_WAKE_LOCK 才能保证 dwg2dxf / Python 抽取持续推进。 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- v1.9.19 后台统计根治③：国产 ROM 默认对未豁免电池优化的 app 在切后台后杀线程/冻结
         子进程(:dwgisolated)，连前台 service 也拦不住。声明后可由 MainActivity 引导用户一键豁免。 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- 注：SAF OpenMultipleDocuments 在所有 Android 版本上均无需任何存储/媒体权限 -->

    <application
        android:name=".WordCountApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="${appName}"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.WordCount">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="${appName}"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 接收千牛/微信等「分享」的文件（SEND / SEND_MULTIPLE） -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/msword" />
                <data android:mimeType="application/vnd.ms-word" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />
                <data android:mimeType="application/vnd.ms-excel" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" />
                <data android:mimeType="application/vnd.ms-powerpoint" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.presentationml.presentation" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="text/*" />
                <data android:mimeType="image/png" />
                <data android:mimeType="image/jpeg" />
                <data android:mimeType="image/*" />
                <data android:mimeType="application/dxf" />
                <data android:mimeType="application/x-dxf" />
                <data android:mimeType="image/vnd.dwg" />
                <data android:mimeType="application/dwg" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- 「用其他应用打开」——让文件管理器/微信/千牛的「打开方式」列表里出现 WordCount -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/msword" />
                <data android:mimeType="application/vnd.ms-word" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />
                <data android:mimeType="application/vnd.ms-excel" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" />
                <data android:mimeType="application/vnd.ms-powerpoint" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.presentationml.presentation" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="application/dxf" />
                <data android:mimeType="application/x-dxf" />
                <data android:mimeType="image/vnd.dwg" />
                <data android:mimeType="application/dwg" />
                <!-- 压缩包 -->
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-rar-compressed" />
                <data android:mimeType="application/rar" />
                <data android:mimeType="application/gzip" />
                <data android:mimeType="application/x-gzip" />
                <data android:mimeType="application/x-tar" />
                <data android:mimeType="application/x-7z-compressed" />
                <!-- 图片 -->
                <data android:mimeType="image/png" />
                <data android:mimeType="image/jpeg" />
                <data android:mimeType="image/*" />
                <!-- 通配兜底 -->
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- 导出 PDF 后通过系统查看器打开 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- v1.5.13: 隔离 LibreDWG 原生 dwg2pdf 调用到独立进程(:dwgisolated)，
             防止 Cad 文件触发 native SIGSEGV 时连带杀掉主 app（闪退）。
             该进程不初始化 Python/Compose，仅加载 libdwg2dxf.so 执行转换。 -->
        <service
            android:name=".DwgIsolatedService"
            android:process=":dwgisolated"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- v1.9.10: 前台占位 service，仅用于让 MainActivity 切后台时进程保持前台优先级，
             避免 Android 14+ 挂起 dwg2dxf/OLE 等耗时 IO 子进程 -->
        <service
            android:name=".WordCountForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- v1.9.25: 独立前台统计进程。实际统计工作在这里跑，规避 Activity 主进程切后台被
             系统冻结导致统计中断。持前台优先级 + 唤醒锁，结果写入外部缓存供主进程恢复。 -->
        <service
            android:name=".CountingService"
            android:process=":countservice"
            android:foregroundServiceType="dataSync"
            android:exported="false" />
    </application>

</manifest>
